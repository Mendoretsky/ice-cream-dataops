[tool.poetry]
name = "poetry add cognite-toolkit"
version = "poetry run cdf --version"
description = ""
authors = ["Mendel"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
